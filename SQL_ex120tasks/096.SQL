SELECT V_NAME
FROM utV
WHERE V_ID IN
(SELECT B_V_ID
FROM utB
WHERE B_Q_ID IN (SELECT DISTINCT B_Q_ID
FROM utB JOIN utV ON B_V_ID=V_ID
WHERE V_COLOR='B')
INTERSECT
SELECT B_V_ID 
FROM utB
GROUP BY B_V_ID
HAVING COUNT(*)>1) AND V_COLOR='R'
